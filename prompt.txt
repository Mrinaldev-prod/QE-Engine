 export PYTHONPATH=.. && python3 ../jira_connector/cli_fetch_jira.py --issue KAN-1 --format json --out ./jira-KAN-1.json
based on the file jira-KAN-2 can you generate test in gherkins format with steps definition under the folder QE-Engine\tests\features\ui considering the context of the framework.
 git clone git@github.com:owner/QE-Engine.git