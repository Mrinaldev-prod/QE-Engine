"""jira_connector package: small utilities to fetch Jira issues from the Jira REST API.

Contains a CLI and a tiny HTTP MCP-compatible server to fetch issues using
credentials supplied via environment variables or a `.env` file.
"""
